#test를 할떄는 test디렉토리 아래에 있는 resources를 먼저 체크한다. 이후 url을 h2 메모리용으로 설정
#근데 스프링 부트가 기본적으로 메모리 모드로 실행하게 해준다. 주석을 해봐도 된다..!
spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
##          show_sql: true
#        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  #org.hibernate.type: trace
